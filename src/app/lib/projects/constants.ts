import { join } from "path";

export const projectMetaFileName = "project.md";
export const imageLocation = "/projects/image-bucket/";
export const pageSize = 10;
export const projectsRoot = join(process.cwd(), "src", "app", "projects");

